# Configuration for Data Bundle Access Service (all profiles)

spring:
  application:
    name: data-bundle-access-service

server:
  port: 8085

# Service-specific configuration
bundle-access:
  download:
    buffer-size: 8192
    compression-level: 6  # ZIP compression level (0-9)
